<% if(actions.default) { actions = actions.default } %>
<% actions.forEach(function(action) { %>
  <% action.link = makeActionUrl(action.link) %>
  <%- render(action.templateMinimal || action.template, action) %>
<% }) %>
