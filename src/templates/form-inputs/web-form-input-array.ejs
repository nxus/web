<div class="form-group">
  <%- render('web-form-input-label') %>
    <div class="col-sm-7">
        <textarea rows="10" style="min-height: 100px;" id="control_<%= name %>" class="form-control" placeholder="Enter <%= label %> as list one item per line" name="<%= name %>"><%= value && value.join ? value.join("\n") : value %></textarea>
    </div>
</div>
